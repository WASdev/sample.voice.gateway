**********************************************************
*          Voice Gateway deployed successfully.          *
**********************************************************

** Command to get IP of deployment:

kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[1].status.addresses[0].address}"
